class SaleStorage
{
    public Dictionary<int, Sale> Sales;
    public int nextSaleId = 1;

    public SaleStorage()
    {
        Sales = [];

//TODO: update the following code to use the Sale class
        // Sale sale1 = new(nextSaleId, 6000, 1579038745, 38000, 1584222745, 0, 0, "Listed", 1584222745); nextSaleId++;
        // Sale sale2 = new(nextSaleId, 18000, 1684101145, 42000, 1684101145, 42000, 1689371545, "Sold", 1689371545); nextSaleId++;

        // Sales.Add(sale1.SaleId, sale1);
        // Sales.Add(sale2.SaleId, sale2);
    }
}
        // LotId = lotId;  
        // AcquiredPrice = acquiredPrice;
        // AcquiredDate = acquiredDate;
        // ListedPrice = listedPrice;
        // ListedDate = listedDate;
        // SoldPrice = soldPrice;
        // SoldDate = soldDate;
        // Status = status;
        // StatusDate = statusDate;
        // SoldBy = soldBy;
        // SoldTo = soldTo;